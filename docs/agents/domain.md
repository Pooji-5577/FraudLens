# Domain documentation

FraudLens uses a single project-wide context.

Before engineering work, read `CONTEXT.md` and any relevant decisions under `docs/adr/`. Use the domain terms defined there in code, tests, issues, and documentation. If an implementation would contradict an ADR, surface the conflict rather than silently overriding it.
